<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Upload Successful - Ikat Digital</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script>
      function copyLink() {
        const linkInput = document.getElementById('downloadLink');
        linkInput.select();
        linkInput.setSelectionRange(0, 99999); // For mobile
        document.execCommand('copy');

        const button = document.getElementById('copyButton');
        button.innerText = '✅ Copied!';
        setTimeout(() => (button.innerText = '📋 Copy'), 2000);
      }
    </script>
  </head>
  <body class="bg-dark text-light d-flex align-items-center min-vh-100">
    <div class="container text-center">
      <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
          <div class="card bg-dark text-light border-secondary shadow-sm p-4">
            <h3 class="mb-3">✅ File Uploaded Successfully</h3>
            <p>Share this link with the recipient:</p>

            <div class="input-group mb-3">
              <input
                type="text"
                id="downloadLink"
                class="form-control bg-dark text-light border-secondary"
                value="<%= downloadUrl %>"
                readonly
              />
              <button
                class="btn btn-outline-light"
                type="button"
                onclick="copyLink()"
                id="copyButton"
              >
                📋 Copy
              </button>
            </div>

            <a href="/" class="btn btn-outline-light mt-3"
              >⬅️ Upload Another File</a
            >
            <!-- Add this where you want to display the QR code -->
            <div class="qr-code-container text-center mt-4">
              <h5>Scan QR Code to Download</h5>
              <img src="<%= qrCodeDataURL %>" alt="Download QR Code" class="img-fluid" style="max-width: 200px;">
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
