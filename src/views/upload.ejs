<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Upload File - Ikat Digital</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-dark text-light d-flex align-items-center min-vh-100">
    <div class="container">
      <div class="row justify-content-center mb-4">
        <div class="col-md-8 text-center">
          <a href="/" class="text-light text-decoration-none">
            <h1 class="fw-bold mb-2">Ikat Digital</h1>
          </a>

          <p class="text-secondary">
            Kirim file tanpa login — akan terhapus otomatis
          </p>
        </div>
      </div>

      <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
          <div class="card bg-dark text-light border-secondary shadow-sm">
            <div class="card-body p-4">
              <h4 class="text-center mb-4">Upload File</h4>
              <form
                action="/upload"
                method="POST"
                enctype="multipart/form-data"
              >
                <div class="mb-3">
                  <label class="form-label">Pilih File</label>
                  <input
                    type="file"
                    name="file"
                    class="form-control bg-dark text-light border-secondary"
                    required
                    onchange="validateSize(this)"
                  />
                </div>

                <div class="mb-3">
                  <label class="form-label">Catatan (opsional)</label>
                  <textarea
                    name="note"
                    class="form-control bg-dark text-light border-secondary"
                    rows="2"
                    placeholder="Tuliskan pesan jika ada..."
                  ></textarea>
                </div>

                <!-- <div class="mb-3">
                  <label class="form-label">Email Penerima (opsional)</label>
                  <input
                    type="email"
                    name="recipientEmail"
                    class="form-control bg-dark text-light border-secondary"
                    placeholder="nama@email.com"
                  />
                </div> -->

                <div class="mb-3">
                  <label class="form-label">Batas Waktu File</label>
                  <select
                    name="expiresIn"
                    class="form-select bg-dark text-light border-secondary"
                  >
                    <option value="30m">30 Menit</option>
                    <option value="1h">1 Jam</option>
                    <option value="1d" selected>1 Hari</option>
                    <option value="7d">7 Hari</option>
                    <option value="30d">30 Hari</option>
                  </select>
                </div>

                <div class="mb-4">
                  <label class="form-label"
                    >Hapus setelah sekali download?</label
                  >
                  <div class="form-check">
                    <input
                      class="form-check-input"
                      type="radio"
                      name="deleteAfterDownload"
                      value="yes"
                      id="onceYes"
                      checked
                    />
                    <label class="form-check-label" for="onceYes"
                      >Ya, hapus setelah sekali diunduh</label
                    >
                  </div>
                  <div class="form-check">
                    <input
                      class="form-check-input"
                      type="radio"
                      name="deleteAfterDownload"
                      value="no"
                      id="onceNo"
                    />
                    <label class="form-check-label" for="onceNo"
                      >Tidak, tetap bisa diunduh sampai waktu habis</label
                    >
                  </div>
                </div>

                <button type="submit" class="btn btn-primary w-100">
                  Kirim File
                </button>
              </form>
            </div>
          </div>
          <p class="text-center mt-3 text-secondary small">
            File akan otomatis terhapus setelah diunduh atau waktu berakhir.
          </p>
        </div>
      </div>
    </div>
  </body>
  <script>
    function validateSize(input) {
      const file = input.files[0];
      if (file && file.size > 50 * 1024 * 1024 * 1024) {
        alert('The selected file exceeds the 50GB limit.');
        input.value = '';
      }
    }
  </script>
</html>
